# Base URL for the Express backend API
EXPO_PUBLIC_API_BASE_URL=http://localhost:5000/api

# (Optional) Override competition ID to load on launch.
# If omitted or left blank, the app automatically fetches the latest seeded competition via GET /api/competitions/latest.
# EXPO_PUBLIC_DEFAULT_COMPETITION_ID=6ab43aaaabd4bffa981a53c9

